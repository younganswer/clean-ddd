get:
    summary: List inventory reservations by order id
    parameters:
        - in: query
          name: orderId
          required: false
          schema:
              type: string
    responses:
        "200":
            description: OK
            content:
                application/json:
                    schema:
                        $ref: "../components.yaml#/schemas/InventoryReservationListEnvelope"
