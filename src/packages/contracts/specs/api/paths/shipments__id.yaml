get:
    summary: Get shipment
    parameters:
        - in: path
          name: id
          required: true
          schema:
              type: string
    responses:
        "200":
            description: OK
            content:
                application/json:
                    schema:
                        $ref: "../components.yaml#/schemas/ShipmentSummaryNullableEnvelope"
