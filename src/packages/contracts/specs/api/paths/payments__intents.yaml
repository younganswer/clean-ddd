get:
    summary: List payment intents
    parameters:
        - in: query
          name: limit
          schema:
              type: integer
              default: 20
              minimum: 1
              maximum: 50
    responses:
        "200":
            description: OK
            content:
                application/json:
                    schema:
                        $ref: "../components.yaml#/schemas/PaymentIntentListEnvelope"
