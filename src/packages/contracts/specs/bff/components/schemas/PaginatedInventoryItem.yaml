type: object
required: [items, offset, limit, total, totalPages, hasNext]
properties:
    items:
        type: array
        items:
            $ref: "../../components.yaml#/schemas/InventoryItem"
    offset:
        type: integer
    limit:
        type: integer
    total:
        type: integer
    totalPages:
        type: integer
    hasNext:
        type: boolean
