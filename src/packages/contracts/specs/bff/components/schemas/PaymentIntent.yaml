type: object
required: [paymentId, orderId, amount, currency, status]
properties:
    paymentId:
        type: string
    orderId:
        type: string
    amount:
        type: number
    currency:
        type: string
    status:
        type: string
