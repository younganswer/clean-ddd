type: object
required: [eventType, delaySeconds, outboxId]
properties:
    eventType:
        type: string
    delaySeconds:
        type: integer
        minimum: 0
    outboxId:
        type: string
