type: object
required: [id, from, to, type]
properties:
    id:
        type: string
    from:
        type: string
    to:
        type: string
    type:
        type: string
    label:
        type: string
